# CI is the same gate as local: scripts/verify.sh, and nothing else.
# Adding a second lint/test/build job here is how CI and the harness drift apart.
name: CI

on:
  pull_request:
  push:
    branches: [main]

# The run reads the repo, builds it, and writes nothing back.
permissions:
  contents: read

# One run per ref: a new push supersedes the one still going.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Node 24, not the 20 the ticket asked for: jsdom 30 (and its undici) needs
      # >= 22.22.2, so on 20 every test file fails to import and TEST is red
      # before it runs a single assertion. package.json engines says the same.
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - run: npm ci

      # TYPES → LINT → TEST → CONTENT → BUILD; one line when green, one failure
      # block when it isn't.
      - name: scripts/verify.sh
        run: bash scripts/verify.sh

      # Red only. Each step's full output lives in .verify/<step>.log, which is
      # gitignored — without this, the 20-line tail in the log above is all a
      # failure leaves behind. include-hidden-files is required: .verify is a
      # dotted path, and v4 drops those silently ("No files were found").
      - name: Upload .verify logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: verify-logs
          path: .verify/
          include-hidden-files: true
