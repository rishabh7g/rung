/**
 * Comprehension — two pool items, reveal, gated self-mark, fresh-item retry. Built in #102.
 */
import { ScreenStub } from './ScreenStub.tsx';

export default function ComprehensionScreen() {
  return <ScreenStub title="Comprehension" ticket="#102" />;
}
